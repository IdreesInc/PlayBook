{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "playdate",
            "request": "launch",
            "name": "Playdate: Debug",
            "preLaunchTask": "${defaultBuildTask}",
            "source": "${workspaceRoot}/source",
            "output": "${workspaceRoot}/build/PlayBook.pdx",
        }
    ]
}